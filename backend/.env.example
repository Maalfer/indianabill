APP_NAME="Indiana Bill Gijón"
DEBUG=true
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# Base de datos SQLite (ruta del archivo .db)
DATABASE_URL="sqlite:///./indianabill.db"

# Seguridad — genera un secreto seguro con: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY="cambia_esto_por_un_secreto_seguro"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30
